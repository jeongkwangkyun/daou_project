<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daou.project.model.mapper.PaymentMapper">
	
	<select id="getCoupon" parameterType="String" resultType="couponDto">
		select *
		from coupon
		where user_no = (select user_no from user where user_id =#{user_id})
		and coupon_yn = 'n'
	</select>
	
	<select id="getSaveMoney" parameterType="String" resultType="saveMoneyDto">
		select *
		from save_money
		where user_no = (select user_no from user where user_id =#{user_id})
	</select>
	
	<select id="getPoint" parameterType="String" resultType="pointDto">
		select *
		from point
		where user_no = (select user_no from user where user_id =#{user_id})
		and expiration_yn ='n'
	</select>
	
</mapper>